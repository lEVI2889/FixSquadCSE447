# Keep /api as the browser-facing prefix so all frontend modules share one API contract.
VITE_API_BASE_URL=/api

# Used only by Vite's local development server.
VITE_API_PROXY_TARGET=http://localhost:5000
